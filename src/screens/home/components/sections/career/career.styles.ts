import { styled } from '@theme/theme';

export const CareerSection = styled('section', {
  bg: '$black',
});

export const CareerSectionContent = styled('div', {
  width: '100%',
  position: 'relative',
  display: 'flex',
  flexDirection: 'column',
});

export const CareerSectionTimelineContent = styled('div', {
  width: '100%',
  position: 'relative',
  display: 'flex',
  flexDirection: 'column',
  py: '$10',
  alignItems: 'center',
  justifyContent: 'center',
  overflowY: 'hidden',
});
