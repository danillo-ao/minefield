import { styled } from '@theme/theme';

export const AcademicSection = styled('section', {
  bg: '$black',
});

export const AcademicSectionContent = styled('div', {
  width: '100%',
  position: 'relative',
  display: 'flex',
  flexDirection: 'column',
  pt: '2rem',
});

export const AcademicSectionTimelineContent = styled('div', {
  width: '100%',
  position: 'relative',
  display: 'flex',
  flexDirection: 'column',
  py: '$10',
  alignItems: 'center',
  justifyContent: 'center',
  overflowY: 'hidden',
});
