import { styled } from '@theme/theme';

export const HomeScreen = styled('div', {
  width: '100vw',
  height: '100vh',
  flex: 1,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
});
